# Your Sweetheart — 游戏玩法机制详解

## 一、核心机制：视野与注视

### 1.1 视野系统 (Vision System)

玩家拥有一个圆形视野区域（类似手电筒），通过鼠标/触控控制视野位置。

| 参数 | 值 | 说明 |
|------|-----|------|
| `VISION_RADIUS_RATIO` | 0.18 | 视野半径为屏幕较短边的 18% |
| `VISION_RADIUS_MIN` | 100px | 视野半径下限 |
| 遮罩透明度 | 0.97 | 视野外几乎全黑 |
| 辉光颜色（默认） | `#333333` | 暗灰色光环 |
| 辉光透明度 | 0.25 | — |

- 视野由自定义 GLSL Shader（[LightingFilter.js](js/shaders/LightingFilter.js)）渲染，使用 `smoothstep` 实现软边光照衰减
- 视野光圈包含双层辉光环（Ring），紧贴视野边界，确保光圈与视野完美对齐
- 视野半径变化有平滑过渡（lerp 速度 0.08/帧），不是瞬间切换

### 1.2 失败条件：离开视野计时器

NPC 少女离开玩家视野时开始计时，超过上限即游戏结束。

| 参数 | 值 | 说明 |
|------|-----|------|
| `MAX_OUT_OF_VISION_MS` | 6000ms (6秒) | 最大允许离开视野时间 |
| `WARNING_THRESHOLD_MS` | 2000ms (2秒) | 剩余时间不足时触发警告效果 |

- **计时器不自动归零**：NPC 回到视野后，计时器冻结在当前值，不会自动减少
- **警告效果**：剩余 ≤ 2 秒时，顶部计时器文字会闪烁（红色/浅红交替，字号在 28-34px 间跳动，频率约 8Hz）
- **恋爱状态减缓**：NPC 处于"示爱"状态时，离开视野的计时速度降至正常的 0.7 倍
- 计时器始终显示在屏幕顶部中央，格式为 `离开视野: X.Xs`

### 1.3 游戏结束 (Jumpscare) 序列

当离开视野计时器达到 6 秒，触发三段式惊吓序列：

| 阶段 | 时间 | 效果 |
|------|------|------|
| Phase 1: 红色闪烁 | 0–200ms | 全屏红色快速闪烁（正弦波控制透明度 0–0.8） |
| Phase 2: 转场 | 200–500ms | 红色消失，惊吓画面出现 |
| Phase 3: 屏幕震动 | 500–3000ms | 全屏血红 + 黑色 Vignette 遮罩 + 随机抖动（幅度从 12px 衰减到 0） |

惊吓画面内容：
- 血红色背景 (`#8b0000`)
- Vignette 暗角遮罩（中心椭圆形镂空）
- 一对血红色大眼睛 + 黑色瞳孔
- 红色弧形咧嘴笑
- 文字「捉到你了 ♥」，带脉冲缩放效果
- 1.5 秒后出现「重新开始」按钮

点击「重新开始」按钮后，所有游戏状态完全重置。

---

## 二、NPC 少女 — 病娇 AI

### 2.1 基础属性

| 属性 | 值 |
|------|-----|
| 显示高度 | ~100px（`TARGET_WIDTH = 100`） |
| 移动速度 | 120 px/s |
| 精灵锚点 | (0.5, 0.85) — 锚点偏下，脚底为基准 |
| 精灵素材 | `assets/girl.png`（单张合并精灵图） |

### 2.2 动画系统

- **方向判定**：根据位移量判断四方向（上下左右），向左移动通过水平翻转右移精灵实现
- **移动浮动 (Bob)**：移动时 NPC 上下浮动，正弦波驱动
  - 振幅 `BOB_AMPLITUDE = 5px`
  - 频率 `BOB_FREQ = 2.5Hz`
  - 停止时平滑回弹（lerp 速度 `BOB_RETURN = 6`）

### 2.3 动态影子

NPC 脚下有一个动态投射影子，始终朝向玩家视野/鼠标方向：

- 影子由 5 层重叠椭圆组成，越靠近鼠标方向的层越小、越透明
- 拉伸距离随鼠标距离变化（最远 350px 时达最大拉伸 110px）
- 鼠标越远，影子单侧拉伸越明显

### 2.4 NPC 逐像素点光源 (NpcLightingFilter)

NPC 精灵身上应用了自定义 Shader，模拟玩家手电筒照在 NPC 身上的效果：

- 光源位置 = 玩家视野中心
- 聚光半径 = `visionRadius * 0.9`（最小 160px）
- 亮斑区域提亮至 1.35 倍，暗部压至 0.04 倍
- 有清晰的明暗交接线（`smoothstep` 过渡带）

### 2.5 AI 状态机 (RandomAI)

NPC 使用状态机驱动行为，每帧基于概率和计时器切换状态：

#### 状态一览

| 状态 | 移动速度 | 持续时间 | 触发概率 | 行为 |
|------|---------|----------|---------|------|
| **WANDER** (漫步) | 120 px/s | 1.0–3.5s | ~50% | 随机方向漫步，方向有微小随机漂移 |
| **IDLE** (静止) | 0 | 0.4–1.6s | ~12% | 完全静止不动 |
| **SPRINT** (冲刺) | 300 px/s (2.5×) | 0.3–1.0s | ~10% | 朝随机方向快速冲刺 |
| **LOVE** (示爱) | 0 | 3–6s | ~8% | 静止示爱，触发视野缩小和心形粒子 |

**状态切换逻辑**（状态计时器归零时触发）：
- 20% 概率进入 IDLE → 其中 40% 转变为 LOVE，60% 为普通 IDLE
- 10% 概率进入 SPRINT
- 其余进入 WANDER

#### AI 微调行为

- **偶尔看向玩家** (2%/帧)：漫步/冲刺时，方向会略微朝向玩家偏移（偏移量 0.06）
- **偶尔走向道具** (3%/帧)：漫步/冲刺时，方向会向最近的道具位置偏移（偏移量 0.04，冲刺时 0.08）
- LOVE 状态下 NPC 完全无视玩家和道具，保持静止

---

## 三、示爱状态 (Love State)

当 NPC AI 进入 LOVE 状态时触发，是游戏的核心情绪高潮设计：

### 3.1 触发与结束

| 参数 | 值 | 说明 |
|------|-----|------|
| 触发方式 | AI 概率触发 | IDLE 时有 40% 概率进入 LOVE |
| 持续时间 | 3–6 秒 | 随机范围 |
| 结束后保持 | `POST_LOVE_HOLD = 3s` | 结束后视野缩小状态额外保持 3 秒 |

### 3.2 效果

- **视野持续缩小**：只要 NPC 在视野内，视野半径以 22px/s 的速度缩小（最小 `LOVE_MIN_RADIUS = 35px`）
- **辉光变色**：视野光圈变为粉红色 `#ff6699`，透明度提升至 0.45
- **心形粒子**：NPC 头顶冒出粉红色心形粒子（每 80ms 生成一个，向上偏左右随机散射，受重力影响下落）
- **地面文字**：延迟 0.5 秒后在地面生成「看着我」文字
- **计时器颜色**：顶部计时器变为粉红色（当剩余时间充足时）
- **离开视野减缓**：LOVE 状态期间离开视野的计时速度降至 0.7 倍

### 3.3 结束

- LOVE 计时结束 → AI 退回 WANDER 状态
- 视野光圈回到暗灰色
- 视野半径在 3 秒内平滑恢复到正常大小
- 心形粒子停止生成

---

## 四、道具系统

### 4.1 道具生成 (Item Spawner)

| 参数 | 值 | 说明 |
|------|-----|------|
| `SPAWN_MIN` / `SPAWN_MAX` | 5–8 秒 | 生成间隔随机范围 |
| `MAX_ITEMS` | 10 | 场上最多同时存在 10 个道具 |
| `MIN_DIST_BETWEEN` | 80px | 道具之间最小间距 |
| `MIN_DIST_FROM_VISION` | 30px | 生成位置必须在视野外（视野半径 + 30px 外） |

道具生成时伴随恐怖文字出现在道具位置（[ItemSpawner.js:10-16](js/ItemSpawner.js#L10-L16)）：
- 文字从预定义池中随机选取，如「嘻嘻」「找到你了」「来玩吧」「别走」「好痛」「救我」等
- 30% 概率将文字中的一个字符替换为随机 Unicode 乱码
- 文字持续 1.8 秒，始终可见（不限于视野内）

### 4.2 道具类型与激活

所有道具通过**注视**（将视野中心对准道具）来激活。视野对准道具时显示进度环，注视满阈值后触发效果。

| 道具 | 生成概率 | 注视阈值 | 视觉表现 | 激活效果 |
|------|---------|---------|---------|---------|
| **Heart（心形）** | 35% | 0.5s | 粉红色贝塞尔心形 | **恢复 2 秒**离开视野计时器 |
| **Bottle（瓶子）** | 35% | 0.5s | 绿色圆角瓶 | **酒醉摇摆 5 秒** — 视野跟随鼠标附近随机徘徊的锚点，移动大幅滞后（lerp 0.18） |
| **Box（道具箱）** | 30% | 1.0s | 红色宝箱精灵图 | 打开宝箱，弹出一个新道具（50–120px 外，贝塞尔弧线飞行动画 0.55 秒），**8% 概率改为生成幽灵** |

- 道具只在视野内可见
- 注视进度不保留：视线移开后以 0.3× 速率回退
- 激活后道具消失
- 宝箱生成的新道具类型概率：10% 再次宝箱、45% 瓶子、45% 心形

### 4.3 肖像道具 (Portrait — 已废弃生成但代码保留)

原计划存在的第四种道具，当前代码中 `_rollType()` 不再生成，但效果代码仍在：
- 随机变换鼠标方向映射：X轴反转、Y轴反转、双轴反转、XY轴交换
- 持续 2 秒

---

## 五、墓碑与幽灵系统

### 5.1 墓碑 (Tombstone)

| 参数 | 值 | 说明 |
|------|-----|------|
| `TOMBSTONE_SPAWN_MIN` / `MAX` | 3–5 秒 | 生成间隔 |
| `MAX_TOMBSTONES` | 5 | 场上最多墓碑数 |
| `TOMBSTONE_THRESHOLD` | 0.5s | 注视激活时间 |
| 生成位置 | 视野外 +30px | 且与其他墓碑间距 ≥ 70px |

- 墓碑视觉：灰色石碑 + 弧形顶部 + "RIP" 文字
- 生成时地面出现文字「安息吧」，持续 1.8 秒，始终可见
- 注视墓碑时显示灰色进度环，满 0.5 秒后墓碑消失，生成一只幽灵
- 墓碑仅在视野内可见

### 5.2 幽灵 (Ghost)

| 参数 | 值 | 说明 |
|------|-----|------|
| `GHOST_SPEED` | 70 px/s | 追寻玩家的速度 |
| `EXPLOSION_RADIUS` | 120px | 接近玩家该距离时开始攻击 |
| `ATTACK_DURATION` | 0.6s | 攻击前蓄力时间 |
| `GHOST_TIMER_PENALTY` | 500ms | 幽灵在视野内爆炸时增加的惩罚时间 |
| 拖尾长度 | 14 帧 | 半透明圆形拖尾 |

**幽灵行为阶段**：

1. **Seeking（追寻）**：以 70px/s 速度直线飞向玩家位置，骷髅头朝向玩家，绿色拖尾
2. **Attacking（攻击）**：接近到 120px 内时进入攻击，0.6 秒蓄力——
   - 骷髅从白色渐变到红色 (`tint` 从 `0xffffff` → `0xff0000`)
   - 体积膨胀至 3.2 倍
   - 随机抖动 0–7px
   - 拖尾变为红色
3. **Exploding（爆炸）**：蓄力完成瞬间——
   - 触发血浆喷溅效果
   - 如果爆炸时幽灵在视野内，离开视野计时器 **+500ms**
   - 幽灵销毁

- 幽灵显示策略：追踪/攻击/爆炸状态始终可见，否则仅在视野内或距离玩家 ≤ 120px 时可见

---

## 六、视觉与氛围系统

### 6.1 地砖地面 (Ground Renderer)

使用程序化生成的棋盘格纹理 + 法线贴图实现动态光影地面：

| 参数 | 值 |
|------|-----|
| 纹理尺寸 | 256×256px |
| 格数 | 8×8（每格 32px） |
| 缝隙宽度 | 1px |
| 颜色 | 深色 `#1a1a1a` / 浅色 `#2c2c2c` |
| 铺满方式 | TilingSprite，缩放 1.5× |

[GroundLightingFilter.js](js/shaders/GroundLightingFilter.js) Shader 效果：
- 基于法线贴图计算逐像素 Lambert 光照
- 光照方向指向玩家视野中心
- 视野中心法线强度最高，向边缘逐渐退化为平面光照
- 纹理对比度中心最高，边缘向灰色淡化
- 环境光 0.03，确保视野外不完全黑
- Wrap 光照（`NdotL * 0.55 + 0.45`），避免背面完全黑暗

### 6.2 CRT 后处理 (Screen Effects)

全屏后处理滤镜，持续运行：

- **噪点颗粒**：随机噪声，随时间流动（频率 90Hz）
- **暗角 (Vignette)**：四角变暗（强度 0.3）
- **色调偏移**：R 通道 ×0.97，B 通道 ×0.90（微弱绿色偏移）
- **亮度微动**：正弦波亮度波动（幅度 ±0.8%，频率 8Hz）
- 使用 `PIXI.Filter` 直接应用在 stage 上

### 6.3 血浆粒子系统 (Blood Splatter)

幽灵爆炸时触发，两阶段粒子效果：

**飞行阶段**（物理粒子）：
- 主喷溅：30–45 个快速径向粒子（速度 100–380px/s，上偏）
- 次级溅射：10–20 个较慢粒子
- 中心滴落：5–8 个大尺寸慢速粒子
- 受重力影响（`GRAVITY = 400`），空气阻力 0.965/帧
- 颜色从 10 种暗红色中随机选取
- 生命周期结束前 20% 淡出

**污染阶段**（永久地面痕迹）：
- 粒子生命结束后在地面留下椭圆形血渍
- 透明度 0.15–0.5
- 40% 概率在周围生成卫星小血渍

### 6.4 心形粒子 (Heart Particles)

LOVE 状态期间特效：

- 生成速率：每 80ms 一个
- 粒子寿命：1200ms
- 发射角度：向上偏左右 ±0.75 弧度（约 ±43°）
- 初始速度：20–60px/s
- 受轻微重力（30px/s²）
- 从 0.5 倍缩放到消失
- 颜色：`#ff3388`

---

## 七、开场流程

游戏启动后的初始序列：

| 时间 | 事件 |
|------|------|
| 0s | 游戏开始，NPC 出现在屏幕中央，静止不动 |
| 0–1s | **起始延迟 (`_startDelay = 1.0`)**：玩家视野固定在屏幕中央，NPC 被冻结 |
| 1s | 起始延迟结束，触发 **intro lerp**（0.5 秒平滑过渡）：视野从中央平滑移动到实际鼠标位置 |
| 1.2s | NPC 头顶出现第一段文字「达令，你要永远注视着我哦」（26px，持续 3 秒，始终可见） |
| ~1.5s | 视野完全跟上鼠标，游戏正式开始，NPC AI 开始运作 |

---

## 八、图层渲染顺序

游戏使用多层 PIXI.Container 构建场景，从下到上：

| 层级 | 容器名 | 内容 |
|------|--------|------|
| 1 | `background` | 纯黑底色 + 法线地砖（带 GroundLightingFilter） |
| 2 | `bloodLayer` | 血浆粒子 + 永久血渍 |
| 3 | `groundTextLayer` | 地面文字（视野遮罩下方，仅视野内可见） |
| 4 | `tombstoneLayer` | 墓碑 |
| 5 | `ghostLayer` | 幽灵（骷髅头） |
| 6 | `itemLayer` | 道具 |
| 7 | `npcLayer` | NPC 少女（含动态影子和 NpcLightingFilter） |
| 8 | `particleLayer` | 心形粒子 |
| 9 | `overlay` | 视野黑色遮罩（含 LightingFilter，挖出视野光圈） |
| 10 | `groundTextOverlay` | 地面文字（遮罩上方，始终可见） |
| 11 | `jumpscareLayer` | 游戏结束惊吓画面 |
| 12 | `uiLayer` | 顶部离开视野计时器 |

---

## 九、输入处理

- **鼠标**：`mousemove` 事件，将 client 坐标转换为 PixiJS 屏幕坐标
- **触控**：`touchstart` + `touchmove`（`preventDefault` 阻止滚动），使用第一个触点
- **光标隐藏**：CSS `cursor: none`，全程不显示系统光标
- **禁止选择**：`user-select: none`，`-webkit-tap-highlight-color: transparent`
- **窗口缩放**：`resize` 事件重新计算视野半径、地面尺寸、滤镜参数

---

## 十、游戏状态与重置

### 状态机

- `playing`：正常运行，每帧执行 `_update()`
- `gameover`：游戏结束，停止更新，显示惊吓画面

### 完全重置 (restart)

点击「重新开始」后执行完整重置：
- 离开视野计时器归零
- NPC 回到屏幕中央
- 视野恢复到初始大小和位置
- 所有道具清除
- 所有墓碑和幽灵清除
- 所有粒子（心形、血浆）清除
- 所有地面文字清除
- AI 状态重置
- 道具效果（鼠标修改器、酒醉偏移）清除
- 起始延迟和 intro lerp 重新开始

---

## 十一、关键数值速查表

| 参数 | 数值 | 位置 |
|------|------|------|
| 最大离开视野时间 | 6s | [Game.js:17](js/Game.js#L17) |
| 警告阈值 | 2s | [Game.js:18](js/Game.js#L18) |
| 视野半径比例 | 18% 屏幕短边 | [Game.js:19](js/Game.js#L19) |
| 视野最小半径 | 100px | [Game.js:20](js/Game.js#L20) |
| LOVE 最小视野 | 35px | [Game.js:21](js/Game.js#L21) |
| LOVE 计时减缓 | 0.7× | [Game.js:22](js/Game.js#L22) |
| LOVE 结束后保持 | 3s | [Game.js:23](js/Game.js#L23) |
| NPC 移动速度 | 120 px/s | [NPC.js:9](js/entities/NPC.js#L9) |
| NPC 冲刺速度 | 300 px/s (2.5×) | [RandomAI.js:69](js/ai/RandomAI.js#L69) |
| 幽灵速度 | 70 px/s | [Ghost.js:2](js/entities/Ghost.js#L2) |
| 幽灵爆炸半径 | 120px | [Ghost.js:4](js/entities/Ghost.js#L4) |
| 幽灵爆炸惩罚 | +500ms | [Game.js:27](js/Game.js#L27) |
| 道具生成间隔 | 5–8s | [ItemSpawner.js:3-4](js/ItemSpawner.js#L3-L4) |
| 道具上限 | 10 | [ItemSpawner.js:5](js/ItemSpawner.js#L5) |
| 墓碑生成间隔 | 3–5s | [Game.js:24-25](js/Game.js#L24-L25) |
| 墓碑上限 | 5 | [Game.js:26](js/Game.js#L26) |
| 墓碑激活时间 | 0.5s | [Tombstone.js:1](js/entities/Tombstone.js#L1) |
| 起始延迟 | 1s | [Game.js:57](js/Game.js#L57) |
| Intro 过渡时间 | 0.5s | [Game.js:274](js/Game.js#L274) |
| 地面文字默认持续时间 | 2s | [GroundText.js:1](js/effects/GroundText.js#L1) |
| 心形粒子寿命 | 1.2s | [HeartParticles.js:1](js/effects/HeartParticles.js#L1) |
| 酒醉效果持续 | 5s | [ItemEffects.js:127](js/ItemEffects.js#L127) |
| 宝箱开启动画 | 0.55s | [ItemEffects.js:3](js/ItemEffects.js#L3) |
| CRT 噪点频率 | 90Hz | [ScreenEffects.js:18](js/effects/ScreenEffects.js#L18) |
